pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                sh 'git clone https://github.com/thangacodes/dotnet_webapp.git'
            }
        }
        stage('InfraBuild') {
            steps {
                   sh '''
                      echo "navigating to the Infrabuild_scripts"
                      cd Infrabuild_scripts/
                      terraform init
                      terraform validate
                      terraform plan -var-file terraform.tfvars
                   '''
            }
        }
            stage('DotnetBuild'){
                steps{
                    sh '''
                    cd super-service/
                    tree
                    docker build -t super-service .
                    docker images
                    docker run --name webapp -d -p 8080:80
                    sleep 3
                    docker ps
                    '''
        }
    }
        // stage('Clean Workspace') {
        //     steps {
        //         cleanWs()
        //     }
        // }
    }
}
